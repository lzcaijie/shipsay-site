# V5.1_TEMPLATE_STANDARD（Shipsay 模板标准草案）

> 目的：这份文档不是给“人看着热闹”的，而是给后续 **AI / 人工快速判断能不能改、该怎么改、哪些不能乱改** 用的。
>
> 适用范围：`shipsay-site` 分站侧模板标准，当前以 `themes/shipsay/` 作为母模板基线；其它主题（如 `88ds`）可参考，但默认不反向覆盖母模板规则。
>
> 当前状态：**v5.1 草案**。本版以“删减 v5.0 里过硬、容易误改的规则 + 补齐真实链路 + 明确验收方式”为主。

---

## 1. 单一目标

后续模板整理的主要目标只有一个：

**让 AI 或人工在看到源码后，能快速、安全地复制一套新模板，而不是为了改模板把原来正确的逻辑改坏。**

所以本标准优先级如下：

1. **不破坏现有正确逻辑**
2. **变量来源准确**
3. **模板职责清晰**
4. **页面显示正常，移动端优先**
5. **在不破坏页面协调性的前提下做统一**

---

## 2. 核心原则（必须遵守）

### 2.1 不能为了“按标准改”而把原来正确的东西改坏

这条优先级最高。

出现以下情况时，默认 **不改**：
- 页面已经显示正常
- 当前变量链路已经正确
- 当前 SEO 已由系统统一输出
- 当前页面结构与程序职责一致
- 改动只会“更像标准”，但没有真实收益

### 2.2 模板不能写死后台可变链接

凡是后台可改或程序可生成的链接，模板里都不能写死。例如：
- `fake_top`
- `fake_recentread`
- `fake_info_url`
- `fake_indexlist`
- 分类链接
- 搜索链接

### 2.3 SEO 优先服从程序实际输出链路

系统中已经存在：
- `shipsay/seo.php`
- `shipsay/configs/seo_tpl.php`

因此模板默认不能再为了“看起来完整”去硬写：

```html
<title>分站1</title>
<meta name="keywords" content="分站1">
<meta name="description" content="分站1">
```

正确规则：
1. 先复用系统现有 SEO 链路
2. 模板只负责输出已经准备好的 `seo_title / seo_keywords / seo_description`
3. 只有在程序未提供时，才允许模板给极轻的兜底值
4. 不允许模板层强行覆盖公共 SEO 逻辑
5. `tpl_recentread.php` 与 `tpl_error.php` 当前阶段只要求可用，不作为 SEO 优先强化页
6. `tpl_top.php` 可先在模板层做 SEO 兜底，但 `shipsay/seo.php` 与 `shipsay/configs/seo_tpl.php` 的总控联调留到模板整体测试阶段

### 2.4 页面添加内容必须与页面气质一致

加模块不是越多越好。所有新增内容都必须满足：
- 与当前模板风格一致
- 不打断主阅读流
- 不造成空白异常或布局失衡
- 手机端能正常显示

---

## 3. 文档与真源约定

### 3.1 文档真源固定在根目录 `docs/`

保留并继续维护：
- `docs/CHANGELOG.md`
- `docs/OPS.md`
- `docs/PROJECT_STRUCTURE.md`
- `docs/V5_1_TEMPLATE_STANDARD.md`
- `docs/VARIABLE_MAP.md`

### 3.2 `shipsay/docs/` 视为废弃目录

`shipsay/docs/` 不是正式文档目录，后续默认：
- 不再维护
- 不再同步
- 不纳入修改包与 A/B
- 允许从仓库与线上清理

正式文档真源仅保留 `root/docs/`。

---

## 4. 模板职责总表

## 4.1 母模板基线

当前母模板基线：
- `themes/shipsay/`

其它主题：
- 允许参考
- 允许局部验证
- 不作为默认标准覆盖母模板

## 4.2 标准模板文件集合

### 必需文件
- `tpl_header.php`
- `tpl_footer.php`
- `tpl_home.php`
- `tpl_category.php`
- `tpl_info.php`
- `tpl_indexlist.php`
- `tpl_reader.php`
- `tpl_search.php`
- `tpl_author.php`
- `tpl_error.php`
- `tpl_recentread.php`
- `tpl_rank.php`
- `tpl_top.php`

### 扩展文件
- `tpl_rank_list.php`（适合榜单详情独立样式时使用）
- `user/*`（用户中心相关模板）

---

## 5. 公共规则

## 5.1 头部与尾部

### Header 负责
- 站点 logo / 标题
- 搜索入口
- 首页 / 书库 / 完本 / 足迹 等公共入口
- 顶部导航

### Footer 负责
- 底部文案
- 版权区
- 公共统计脚本或站点尾部信息

### 规则
- 模板页统一使用 `__THEME_DIR__` include
- 静态资源统一走：`/static/<?=$theme_dir?>/`
- 搜索入口必须走程序/配置生成的可变链接，不能写死 `/search/`
- 头部导航链接必须优先走 safe 变量（如 `$allbooks_url_safe`、`$full_allbooks_url_safe`、`$recentread_url_safe`、`$rank_entry_safe`），不能直接信任未兜底变量
- 页面全局数据调用数量优先使用双数；若为栅格/多列模块，默认先按双数收口，避免 PC 端出现空白列。
- 模板中未使用、且会干扰链路判断的历史变量应及时清理，避免后续把“死变量”误当成真实标准。

## 5.2 手机端优先

验收时先看手机端，再看 PC 端。

必须满足：
- 不横向撑破
- 不错位
- 不出现大片无意义空白
- 不因加模块影响主体阅读/点击流
- 按钮可点，搜索可用，分页可用

实现方式要求：
- 优先在现有规则上顺着收口
- 优先复用现有选择器与现有结构
- 能不用 `!important` 就不用
- 不能靠“强制覆盖一整块”来掩盖结构问题

### 手机端通用问题归类（新增）

优先排查并沉淀为通用规则的，不是所有模块，而是**带图片的列表卡片**。

重点包括：
- 左图右文卡片在手机端的错位、挤压、留白、对齐问题
- 图片容器宽高不统一，导致同一列表上下跳动
- 文字容器未设置 `min-width: 0`，导致标题/简介撑破布局
- 标题、简介、作者、字数、更新时间等信息行高不一致
- 按钮、标签、状态条在移动端过宽或过高，造成视觉断裂
- 优先做间距、封面尺寸、标题行高、作者/字数/更新时间一行节奏的微调，不为了一次收口而重写整块结构
- 首页推荐卡、分类列表卡、首页分类首卡、详情/目录顶部书封信息区要尽量沿同一套移动端节奏收口，便于后续模板复用

### 手机端 footer 标准（新增）

底部区域属于全站通用模块，后续默认按“轻量收口”处理，不采用强压覆盖式修法。

标准要求：
- 底部文案整体居中
- 每一行文案在手机端保持居中对齐
- 优先沿用现有 footer 结构调整，不随意重写 HTML
- 优先通过更具体但克制的选择器修正布局，不默认追加大量 `!important`
- footer 的修法要能复用到首页、分类页、详情页、目录页、阅读记录页等全站页面
- 若页面已基本正常，只做轻量微调，不为了“看起来更整齐”继续叠加强覆盖样式

### 分类分页块手机端标准（新增）

分类页手机端分页默认使用“上一页 / 页码选择 / 下一页”的纵向收口方式，要求：

- 三个元素纵向排列
- 按钮宽度统一
- 选择框宽度统一
- 不出现中间大块空白
- 不依赖额外模板结构改造，优先兼容程序当前输出的 `$jump_html_wap`

### 手机端低风险区（补充）

- 纯文字列表
- 章节目录纯文本列表
- 阅读页正文区

这类页面通常问题更少，默认优先少动；手机端优化时，先收带图卡片，再回头校准纯文字模块。

## 5.3 标签一致性

页面中的：
- 标题
- 面包屑
- 按钮文案
- 模块标题
- SEO 标题

必须保持一致，不能页面写“月排行榜”，SEO 却还是“总排行榜”。

---

## 6. 各页面职责与标准

## 6.1 首页 `tpl_home.php`

### 必须保留
- 推荐书区
- 最近更新/最新入库之类的核心列表
- 顶部导航与搜索
- 站点基础入口

### 不再强制
- **不强制首页必须嵌入完整排行榜模块**

### v5.1 新规则
- 首页只要求提供 **排行榜入口** 即可
- 是否展示完整排行榜块，由模板风格决定
- 如果首页已经平衡，不要为了“模块齐全”硬塞排行块

## 6.2 栏目页 `tpl_category.php`

### 当前目标
- PC 端可分栏
- 手机端单列
- 长标题不撑坏布局

### 规则
- PC 端推荐双列或多列
- 长标题可限制字数 / 两行截断
- 简介区高度要有统一节奏
- 可加入长尾推荐链接，但不能抢主列表视觉

## 6.3 详情页 `tpl_info.php`

### 必须保留
- 封面 / 书名 / 作者 / 分类 / 状态 / 字数
- 最新章节信息
- 开始阅读 / 查看目录
- 作品简介
- 最新章节区
- 长尾相关推荐

### 明确禁止
- 为了“更简洁”把章节区删掉
- 为了“更统一”把相关推荐删掉

### 说明
详情页不是只有“书信息卡片 + 两个按钮”。

## 6.4 目录页 `tpl_indexlist.php`

### 必须保留
- 书籍基础信息
- 目录标题
- 开始阅读 / 返回详情
- 章节列表
- 分页逻辑（如启用分页）

### 规则
- 目录列表不能出现右半边大面积无意义留白
- 章节列表区宽度要贴合主内容
- 手机端目录按钮优先竖排或自然换行
- 目录页长尾相关推荐必须与详情页保持同一套：同标题、同判断条件、同链接字段、同展示字段
- 目录页 SEO 至少要有：canonical、TDK、OG 基础项、BreadcrumbList 结构化数据

## 6.5 阅读页 `tpl_reader.php`

### 必须保留
- 面包屑
- 章节标题
- 作者 / 字数 / 时间 等基础信息
- 正文输出：`<?=$rico_content?>`
- 上一章 / 下一章 / 返回目录 / 返回详情
- 阅读分页信息（如果启用阅读分页）

### 规则
- 正文区必须优先保证阅读体验
- 面包屑、分页、设置按钮不能压主阅读内容
- 不能因为“加功能”破坏正文可读性
- 阅读页 SEO 以当前章节页为准，至少输出：canonical、TDK、OG 基础项、BreadcrumbList 结构化数据
- `og:title / og:description` 优先跟随当前页面 SEO 结果，不再额外拼出一套与 TDK 不一致的文案

## 6.6 搜索页 `tpl_search.php`

### 必须保留
- 搜索表单
- 结果列表
- 结果数量
- 搜索关键词标题一致性

### 规则
- 搜索表单样式要与头部保持一致
- 搜索结果卡片不能比首页/栏目页风格完全脱节

## 6.7 阅读记录 `tpl_recentread.php`

### v5.1 统一规则
- 单独一页，不与排行榜或推荐列表混用
- 头部入口固定保留
- 支持无封面占位
- PC / 手机端使用同一数据逻辑，不是两套含义不同的布局

## 6.8 排行榜 `tpl_rank.php / tpl_top.php / tpl_rank_list.php`

### 统一目标
- 聚合榜入口
- 单榜页
- 榜单切换
- 搜索区
- 手机端自适应

### 新规则
- 后台 `fake_top` 是唯一主入口前缀
- 单榜页链接也必须基于 `fake_top`
- 榜单切换必须自适应
- 不允许模板里继续写死 `/top/` 或 `/rank/`

---

## 7. v5.0 删减项（v5.1 明确收回）

以下条目在 v5.1 中明确弱化或删除：

1. **首页必须有完整排行榜模块** → 改为“首页必须有排行榜入口”
2. **模板默认补 SEO title/keywords/description** → 改为“优先服从系统 SEO 链路”
3. **为统一而删减详情页章节区/相关推荐** → 禁止
4. **只强调模板齐全，不强调页面职责** → 改为“职责优先”

---

## 8. 移动端验收标准

每一轮模板修改都必须过这一组检查：

### 首页 / 列表
- 图片、标题、简介、作者信息对齐正常
- 搜索框不压行、不偏移
- 导航不遮挡主内容

### 详情页
- 简介区无异常留白
- 按钮区位置自然
- 最新章节区可正常阅读

### 目录页
- 章节列表宽度正常
- 按钮与标题区不冲突
- 不留大面积空白

### 阅读页
- 面包屑不过长或挤压主内容
- 分页信息位置合理
- 设置按钮不抢正文阅读区

### 排行榜/搜索/足迹
- 手机端可点击
- 搜索框不变形
- 榜单切换可自适应
- 列表排版不溢出

---

## 9. 修改流程（AI / 人工共用）

每次改模板，按这个顺序：

1. 先确认当前源码包是最新基线
2. 先核变量来源
3. 先核页面职责
4. 先判断当前是否已经正确
5. 再决定改不改
6. 修改后先做基础语法检查
7. 再打包交付

不允许直接“看页面像有问题”就凭印象大改。

---

## 10. v5.1 第一轮建议范围

优先处理：
- `tpl_header.php`
- `tpl_home.php`
- `tpl_category.php`
- `tpl_info.php`
- `tpl_indexlist.php`
- `tpl_recentread.php`

第二批再处理：
- `tpl_reader.php`
- `tpl_rank.php`
- `tpl_top.php`
- `tpl_search.php`

原因：先收口结构与变量，再收口阅读细节与排行榜细节，冲突更少。

---

## 11. 一句话结论

**v5.1 的本质不是“加更多规则”，而是把规则收准：哪些必须保留、哪些不能写死、哪些不能乱删、哪些必须优先服从程序真实链路。**


---

## 7. 本轮新增红线（2026-03-07）

### 7.1 准确性优先于完整性

当前以“完全准确的 Shipsay”作为母模板基线。

规则：
- 文档中的函数、标签、变量定义 **可以暂时缺失**
- 但 **不能写错**
- 缺失项后续可以继续补
- 错误定义一旦进入标准，会被后续所有模板继承放大

### 7.2 前台禁止展示内部实现说明

页面前台禁止直接展示以下类型文案：
- 默认展示……
- 当前逻辑……
- 本页规则……
- 模板内部标准说明……

这类内容只能放在：
- `docs/*.md`
- 必要代码注释
- 内部协作说明

### 7.3 模板不能直接承担数据层职责

标准规则：
- `themes/*` 只负责展示
- `shipsay/app/*` 负责准备页面数据
- `class/include/configs` 负责底层逻辑与公共能力

明确禁止：
- 模板中直接拼 SQL
- 模板中直接读 Redis
- 模板中直接查数据库

当前基线已将排行聚合页的数据准备迁回 `shipsay/app/top.php`，`themes/shipsay/tpl_top.php` 只保留展示职责。后续模板必须沿用这种边界，不得再把榜单查询写回模板。


补充要求：
- 搜索页模板不得直接裸输出 `$searchkey`，必须先做安全转义；需要高亮时，也必须在转义后输出。
- 分类页、头部、排行入口这类公共链接必须优先走 `*_safe` 兜底变量，不能直接假定链路变量一定存在。

### 7.4 文档真源固定

唯一真源固定在根目录：
- `docs/CHANGELOG.md`
- `docs/OPS.md`
- `docs/PROJECT_STRUCTURE.md`
- `docs/VARIABLE_MAP.md`
- `docs/V5_1_TEMPLATE_STANDARD.md`

`shipsay/docs/` 只保留迁移占位，不再放正文。

### 7.5 Shipsay 当前例外与后续统一规则

当前阶段：
- Shipsay 正在作为标准反推母模板
- 如遇到已经核对清楚、且不小改核心就无法准确收口的问题
- **允许 Shipsay 小范围例外修核心**

但从 **下一个模板开始**：
- 默认只能修改当前模板目录下文件
- 不再动 `app / class / include / configs` 等核心文件
- 修改模板前必须先阅读 `docs/` 标准文件

### 7.6 章节、排序、混淆链路必须以真实运行结果为准

涉及章节顺序、目录分页、阅读 URL、混淆 ID 时：
- 不能只看局部代码就下结论
- 必须结合真实运行页验证
- 必须以最终页面输出结果校准定义

这条尤其适用于：
- `tpl_info.php`
- `tpl_indexlist.php`
- `tpl_reader.php`
- 对应 `shipsay/app/*.php` 的章节准备链路

---

## 8. 本轮 SEO 补充标准（2026-03-07）

### 8.1 首页 `tpl_home.php`

首页至少应统一输出：
- `title / keywords / description`
- `canonical`
- `applicable-device / mobile-agent`
- 基础 OG 信息
- `WebSite` 级别结构化数据

### 8.2 分类/作者/排行页

这类可被搜索引擎稳定抓取的列表页，至少应统一输出：
- `title / keywords / description`
- `canonical`
- `applicable-device / mobile-agent`
- 基础 OG 信息
- `BreadcrumbList` 结构化数据

### 8.3 详情/目录/阅读页

详情页、目录页、阅读页已经是核心 SEO 页面：
- 优先沿用现有系统 SEO 链路
- 模板仅做安全兜底和结构统一
- 不允许为“补 SEO”去重写核心逻辑

### 8.4 错误页、阅读记录、搜索页

这几类页面后续可继续细化，但当前优先级低于：首页、分类、作者、排行、详情、目录、阅读。

## 9. 手机端带图区排版补充标准（2026-03-07）

### 9.1 首页 / 分类页带图卡片

手机端带图卡片的默认收口目标：
- 封面与文字顶部对齐
- 标题优先稳定在 1–2 行
- 简介控制在 2–3 行内，不和底部信息互相挤压
- 作者信息与字数 / 更新时间保持同一底部节奏

实现原则：
- 优先沿用现有 `.side_commend / .searchresult / #tempBookcase` 结构
- 优先微调封面宽高、gap、line-height、clamp
- 不为修手机端卡片去重排模板 DOM
- 非必要不使用 `!important` 强压覆盖

### 9.2 首页分类首卡 `sortvisit`

`sortvisit` 首条“左图右文”在手机端容易出现：
- 封面与右侧文案上下节奏不齐
- 作者信息依赖 `<br>` 撑开，导致视觉不稳定
- 简介有效行数偏少，看起来像被截断

标准做法：
- 保持左图右文结构不变
- 标题、作者、简介分成更清晰的三段节奏
- 作者信息单独成行
- 只通过 CSS 收口，不先改模板结构

### 9.3 详情页 / 目录页顶部图文区

手机端 `novel-basic-info` 标准：
- 书封为固定列
- 右侧信息区为自适应列
- 标题、基础元信息、最新章节、按钮区分层清楚
- 作者 / 分类 / 状态 / 字数优先按两列节奏收口

注意：
- 这里的优化属于“图文信息区排版标准”，不是变量标准修改
- 不得为了手机端细调去改动章节、目录、SEO 等核心链路


### 9.4 详情页 / 目录页书封信息区的手机端补充

当作者名、章节名较长时，`novel-basic-info` 手机端不要死守僵硬两列元信息。

默认收口原则：
- 书封区仍保持左图右文，不改模板结构。
- 书名保持单独一层。
- 作者信息优先保证完整可读，必要时单独占一行。
- 分类 / 状态 / 字数可顺势换到下一行，不强求平均分栏。
- 最新章节与时间保留同一区块，但优先可读，不为了“凑整齐”压坏折行。

这条规则的目标不是追求表面完全对称，而是保证手机端图文邻接区的阅读稳定性。

### 9.5 首页分类首卡 `sortvisit` 的手机端补充

首页分类首卡本质上也是“左图右文”带图卡片，只是文案结构更老。

默认收口原则：
- 继续沿用现有 DOM，不为手机端去重排模板结构。
- 封面顶部对齐，右侧文案与封面顶部贴齐。
- 标题、作者、简介必须有明确层级，不能继续依赖 `<br>` 撑视觉。
- 优先通过 `line-height / margin / 封面尺寸 / 文案区宽度` 微调完成适配。
- 不允许用大块强制覆盖把整个首卡“压平”。


### Mobile image-text fit (2026-03-07 addendum)
- 带图卡片在移动端优先固定封面列宽，再让标题、简介、作者与字数/时间构成稳定文字列。
- 首页分类首卡 `sortvisit` 首图条目应与普通带图卡片保持接近的封面宽高比与文字节奏，避免图片过小而文字块过散。
- 详情页、目录页顶部信息区优先采用“固定封面列 + 自适应元信息列”布局，最新章节链接允许单独换行，不强行与时间挤在同一行。


### 9.6 首页 / 分类列表摘要在手机端不要保留首行缩进

首页与分类列表的带图卡片，在手机端属于“窄列 + 图文邻接”场景。

默认规则：
- 列表摘要可以继续保留简介语义，但不要再沿用正文式 `text-indent: 2em`。
- 原因不是风格问题，而是手机端图片占位后，摘要宽度已经明显缩窄；继续首行缩进会让首屏有效字符过少，造成右侧文字块发散。
- 首页 / 分类列表这类卡片摘要，优先按 2 行收口。
- 详情页正文简介仍可保留正常段落缩进；不要把列表摘要规则误套到详情正文。

这条属于移动端图文邻接卡片标准，不涉及变量或核心链路修改。

### 9.7 详情页 / 目录页顶部元信息标签不要裸写在同一文本节点（2026-03-07）

对于 `tpl_info.php`、`tpl_indexlist.php` 顶部 `novel-meta` 区块：

- 不要继续使用 `作者：<a>...</a>` 这类“标签文本 + 内容”混在同一个裸文本节点里的写法去赌移动端折行。
- 推荐改成：`<i class="meta-label">作者：</i><a>...</a>` 这种可单独控制标签宽度与不换行策略的结构。
- `分类 / 状态 / 字数` 同理。
- 目标不是为了加样式类本身，而是为了让手机端与 PC 端都能稳定控制标签与内容的折行边界。

### 9.8 首页分类首卡 `sortvisit` 简介不要再依赖 `<br>` 撑开（2026-03-07）

首页分类首卡首图条目：

- 标题、作者、简介应拆成明确层级。
- 简介应单独包成 `sort-desc` 一类可控节点，便于移动端按 2 行摘要收口。
- 不要继续依赖 `<br>` + 缩进空格去“挤”出简介区；这种写法在窄屏下最容易造成溢出、截断和节奏失控。
- 这条规则属于模板层结构微调，但必须同步兼顾 PC 端，不允许只修手机端导致桌面端首卡断层。


### 9.9 手机端首页 / 分类列表带图卡片摘要可进一步收口为 1 行（2026-03-07）

当封面、标题、作者、字数/更新时间都已经稳定后，首页与分类列表的带图卡片在手机端可以进一步把摘要收成 1 行。

默认规则：
- 仅限手机端窄屏列表卡片。
- 目标优先级是：标题完整度 > 作者与字数/时间对齐 > 摘要存在感。
- 如果“作者行无法安全提到标题后面”，则优先把摘要改成 1 行，而不是继续压缩标题和作者行。
- 这条规则同时适用于首页大神推荐列表与分类列表页带图列表。

### 9.10 手机端详情页按钮与元信息区补充（2026-03-07）

对于 `tpl_info.php` 顶部 `novel-basic-info` 区块：

- 手机端下，书封右侧的元信息区优先与书封顶部和可视高度保持更紧凑的对应关系。
- `开始阅读 / 查看目录` 这组主操作按钮，在手机端允许铺满整行，不必局限在右侧元信息窄列里。
- 这条属于手机端交互优先规则；PC 端保持现有正常布局，不做强行同步变更。

### 9.11 手机端目录页最新章节长链接不要挤爆信息卡（2026-03-07）

对于 `tpl_indexlist.php` 顶部 `novel-meta` 区块：

- 最新章节名较长时，必须允许作为独立可控链接行处理。
- 不要让长章节名把元信息卡横向撑坏或把后续时间文本挤出边界。
- 手机端优先保证“不溢出、可识别、节奏稳定”，再考虑完整展示。

- 手机端收口时必须与 PC 样式分流；详情页/目录页顶部信息区、按钮区若为手机端特化，必须限制在 `max-width: 768px` 范围内，不能带动 PC 节奏回归。


---

## 12. 2026-03-08 | v5 全量核对结论（当前 Shipsay 基线）

### 12.1 当前核对口径

本轮不再按“某个旧模板整页覆盖”处理，而是固定采用下面这条口径：

- **旧的、已验证正常的模板/CSS**：只作为**视觉与布局参考**
- **当前主题中已经符合 v5 的变量链 / safe 链接 / 阅读链路**：必须保留
- **目录页**：必须与详情页保持同一结构体系
- **阅读页**：必须保持“普通用户 JS 加载、蜘蛛直出正文”的双链路

一句话：

**布局可借旧版，变量链不能退回旧版。**

### 12.2 当前页面分级

#### A 级：已基本符合当前 v5，可作为标准页保留
- `tpl_home.php`
- `tpl_info.php`
- `tpl_indexlist.php`
- `tpl_reader.php`

#### B 级：可用，但需要后续继续收口
- `tpl_category.php`
- `tpl_rank.php`
- `tpl_top.php`
- `tpl_recentread.php`

#### C 级：当前先保可用，不作为高优先级精修页
- `tpl_error.php`

### 12.3 本轮确认可保留的结构规则

#### 首页 / 分类页
- 首页、分类页当前已经跑通的列表节奏优先保留
- 后续若修样式，优先回到“旧稳定基底”的正常表现，不再用多轮补丁链继续叠加
- 分类名、分类链接必须继续走 `Sort::ss_sorturl()` / `Sort::ss_sortname()` 或 app 层准备好的真实变量，不能回退成写死分类名

#### 详情页 / 目录页
- 目录页必须和详情页保持同一结构体系
- 顶部信息区、元信息、按钮区、相关推荐判断逻辑要统一
- 目录页不是独立风格页，而是“详情页同体系延伸页”
- 详情页、目录页允许借旧版布局节奏，但不能把旧版错误变量链一起带回来

#### 阅读页
- 普通用户：正文通过 `/api/reader_js.php` 拉取
- 蜘蛛或关闭 JS：直出 `<?=$rico_content?>`
- 该行为属于当前 Shipsay 正式标准，不是临时兼容

### 12.4 硬编码使用边界（本轮明确）

#### 允许写死的 UI 文案
下面这些属于界面固定文案，允许模板直接写：
- 首页
- 分类
- 作者：
- 状态：
- 字数：
- 最新章节：
- 开始阅读
- 查看目录
- 返回详情
- 相关推荐
- 热门小说
- 友情链接

#### 禁止写死的业务数据 / 真实链接
下面这些必须对应后台或程序真实变量，不能模板硬编码：
- 书名
- 作者名
- 分类名
- 详情链接
- 目录链接
- 阅读链接
- 搜索入口
- 排行入口
- 阅读记录入口
- SEO 页面真实 title / keywords / description 数据来源
- 阅读页正文加载方式

### 12.5 分类页属于“待收口页”

`tpl_category.php` 当前数据链路基本正确，但模板写法仍带有旧交互痕迹，后续继续收口时应优先清理：

- `onclick="javascript:..."`
- `href="#"`
- `href="javascript:"`
- 过度依赖模板内 fallback 的旧交互写法

说明：
- 当前这类写法在运行层面未必立刻报错
- 但它们**不是 v5 最终标准写法**
- 后续整理模板时，应优先替换为 app 层真实变量 + safe 链接

### 12.6 错误页 / 最近阅读 / 排行页 / 聚合页分层处理

#### 错误页 `tpl_error.php`
- 当前目标是“可用”
- 不作为高优先级样式精修页
- 后续允许继续存在适量行内兜底，但不鼓励扩散到其它模板页

#### 最近阅读页 `tpl_recentread.php`
- 当前已回到旧稳定布局体系
- 后续重点不是重写，而是文档补齐

#### 排行页 / 聚合页 `tpl_rank.php` / `tpl_top.php`
- 当前允许先保可用
- 后续再逐步收口到统一榜单页标准

### 12.7 2026-03-08 起的重建原则

后续如果再整理 Shipsay 模板，默认顺序固定为：

1. 先确认当前上传源码包是唯一基线
2. 先保住当前 v5 正确变量链 / safe 链接 / 阅读双链路
3. 再借旧的、已验证正常的模板/CSS 做布局参考
4. 目录页必须按详情页同体系处理
5. 已正常的首页 / 分类页尽量不乱动
6. 不再沿用历史补丁链继续叠样式

这条原则同样适用于后续其它模板：

**优先复用已有正确部分，再补当前必须功能；缺失可以后补，错误不能进入标准。**


### 12.8 第二轮收口后的当前基线文件

本轮不再把“某个旧版整页覆盖”当成标准，而是确认当前 Shipsay 基线由下面两类文件共同组成：

#### A. 当前应继续保留的 v5 合规文件/能力
- `tpl_home.php`
- `tpl_info.php`
- `tpl_indexlist.php`
- `tpl_reader.php`
- 当前 safe 链接变量链
- 当前 SEO 输出链路
- 当前阅读页双链路

#### B. 当前允许借旧版节奏/布局的模块
- 详情页顶部信息区与按钮区
- 目录页顶部信息区与按钮区
- 首页 / 分类页图文卡片节奏
- 最近阅读页旧稳定布局

规则：
- 当前模板的**变量、标签、链接链路**优先保留。
- 旧模板的**布局、节奏、模块顺序**只作为样式参考。
- 任何时候都不允许因为“套旧 css”而把当前正确变量链退回旧写法。

### 12.9 第二轮固定规则：目录页与详情页必须同体系

从本轮开始，`tpl_indexlist.php` 的标准地位明确为：

- 目录页不是独立风格页
- 目录页不是临时新增页
- 目录页必须视为**详情页同体系延伸页**

必须保持一致的内容包括：
- 顶部书籍信息区结构
- 作者 / 分类 / 状态 / 字数的标签逻辑
- 主操作按钮区逻辑
- 最新章节信息块逻辑
- 长尾相关推荐判断逻辑

允许不同的部分只有：
- 章节目录列表
- 目录分页本身
- 返回详情按钮文案/入口

### 12.10 CSS 处理方法（第二轮正式固定）

后续再处理 Shipsay 样式时，默认采用下面的方法，不再退回“尾部补丁链”：

1. 先确认当前问题属于哪个模块
   - 首页 / 分类页图文卡片
   - 详情页 / 目录页顶部信息区
   - 阅读页正文区
   - 辅助页（最近阅读 / 排行 / 聚合页）

2. 先查旧版是否存在“已验证正常”的控制块
   - 如果旧版已有正确控制块，优先复用已有正确部分
   - 不再先追加新的 `v5.2.x` 补丁块

3. 当前版若已存在正确变量链，只允许：
   - 调整原控制块的值
   - 合并重复规则
   - 补最小新增支持

4. 明确禁止：
   - 为同一模块连续追加多轮手机端覆盖块
   - 在未清理旧冲突块前，继续追加更高优先级规则
   - 把首页 / 分类页已正常部分顺带改坏
   - 用旧模板整页覆盖当前模板

### 12.11 第二轮页面处理顺序（后续默认）

后续重整理 Shipsay 或其它模板时，默认按下面顺序执行：

1. **先核变量链与 safe 链接**
2. **再核页面结构是否符合当前模板职责**
3. **再借旧版正常布局做样式参考**
4. **最后只补当前必须新增的功能支持**

页面优先级固定为：
1. `tpl_info.php`
2. `tpl_indexlist.php`
3. `tpl_home.php`
4. `tpl_category.php`
5. `tpl_reader.php`
6. `tpl_recentread.php / tpl_rank.php / tpl_top.php / tpl_error.php`

### 12.12 第二轮待整改清单（不等于立即修改）

#### 高优先级待收口
- `tpl_category.php` 的旧交互写法（`onclick` / `javascript:` / `href="#"`）
- 详情页 / 目录页的结构同体系说明继续补全到文档中
- 首页 / 分类页图文卡片若再调整，只能在旧正常控制块内微调

#### 中优先级待收口
- `tpl_recentread.php` 文档说明补齐
- `tpl_rank.php` / `tpl_top.php` 的模块职责与变量表补齐

#### 低优先级待收口
- `tpl_error.php` 的样式与行内兜底继续整理
- 旧 fallback 写法逐步清理，但不以破坏可用性为代价



### 12.13 第三轮固定规则：辅助页分层处理（2026-03-08）

#### 主核心页（优先级最高）
- `tpl_home.php`
- `tpl_category.php`
- `tpl_info.php`
- `tpl_indexlist.php`
- `tpl_reader.php`

规则：
- 这组页面决定模板主观感受与标准基线。
- 后续修改优先先核对文档，再做最小范围修改。
- 其中 `tpl_info.php` 与 `tpl_indexlist.php` 必须继续保持同体系。

#### 次核心页（跟随主核心页风格）
- `tpl_recentread.php`
- `tpl_rank.php`
- `tpl_top.php`

规则：
- 允许在结构上更轻，但风格不得与主核心页断裂。
- 默认复用主模板的通用容器、标题条、列表卡片、分页块，不另起一套独立视觉体系。

#### 可用优先页（先保可用）
- `tpl_error.php`

规则：
- 当前阶段优先保证可用与不误导用户。
- 不要求先达到主模板精修程度。
- 后续统一批量整理时再做视觉收口。

### 12.14 第三轮禁止事项（后续默认生效）

后续整理 Shipsay 或其它模板时，默认禁止：
- 在现有 `style.css` 尾部继续叠“某次截图临时修复块”。
- 把参考模板整页照搬进当前模板，不做变量与结构差异定位。
- 为了追求“像标准”而把当前正确变量链、safe 链接、阅读双链路改坏。
- 在详情页和目录页上分别发明两套结构或两套样式逻辑。
- 把分类页旧交互写法当成最终标准保留不记账；它必须进入待整改清单。

### 12.15 第三轮模板复用顺序（用于后续批量做别的模板）

后续处理其他模板时，默认复用顺序固定为：
1. 先核对 `tpl_info.php` / `tpl_indexlist.php`
2. 再核对 `tpl_home.php` / `tpl_category.php`
3. 再核对 `tpl_reader.php`
4. 最后补 `tpl_recentread.php` / `tpl_rank.php` / `tpl_top.php` / `tpl_error.php`

理由：
- 详情页与目录页决定结构标准；
- 首页与分类页决定列表卡片与分页标准；
- 阅读页决定双链路正确性；
- 辅助页最后再跟随主模板风格收口。

### 12.16 第三轮待整改清单（拆细版）

#### 必须继续记账，但不要求本轮立刻修改
- `tpl_category.php`：`onclick` / `javascript:` / `href="#"` 旧交互写法仍需后续收口。
- `tpl_error.php`：行内样式过多，后续应迁回样式表。
- `tpl_recentread.php`：当前功能可用，但文档与变量说明仍可继续补细。
- `tpl_rank.php` / `tpl_top.php`：当前可用，但后续需要再统一卡片、页头、分页和 tabs 风格。

#### 当前已确认，不再重复折返的问题
- 分类页 PC 分页问题已修复，不再回到旧分页错位链路。
- 详情页“50章预览缺失 / 长尾相关推荐位置错误”已修复，不再回到旧错误输出顺序。
- 阅读页普通用户 JS、蜘蛛直出正文的双链路已确认正确，后续不得误改回普通用户直出正文。
